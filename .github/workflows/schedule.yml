name: Update Trending Playlist

on:
  schedule:
    # 07:00 giờ VN (UTC+7)
    - cron: "0 0 * * *"

    # 15:01 giờ VN (UTC+7)
    - cron: "1 8 * * *"

  workflow_dispatch:

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Check secrets an toàn (không lộ giá trị)
      - name: Verify secrets presence
        run: |
          if [ -z "${{ secrets.YOUTUBE_CLIENT_ID }}" ]; then
            echo "❌ Missing secret: YOUTUBE_CLIENT_ID"
            exit 1
          fi
          if [ -z "${{ secrets.YOUTUBE_CLIENT_SECRET }}" ]; then
            echo "❌ Missing secret: YOUTUBE_CLIENT_SECRET"
            exit 1
          fi
          if [ -z "${{ secrets.YOUTUBE_REFRESH_TOKEN }}" ]; then
            echo "❌ Missing secret: YOUTUBE_REFRESH_TOKEN"
            exit 1
          fi
          echo "✅ All required secrets are present"

      - name: Run playlist update script
        env:
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: python main.py
